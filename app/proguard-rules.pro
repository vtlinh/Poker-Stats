# Default ProGuard rules for the app.
# Compose and AndroidX ship their own consumer rules; the engine and UI use no
# reflection, so no extra keep rules are required.

# Keep line numbers for readable crash reports, hide the source file name.
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
